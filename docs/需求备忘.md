# 安崽ETF — 需求备忘

> 汇总自项目启动对话与已确认规划。后续变更请改本文并注明日期。  
> 最后更新：2026-08-06  
> iOS 全屏设计：[iOS全屏设计.md](./iOS全屏设计.md)  
> 交互小细节基础：[交互基础规范.md](./交互基础规范.md)  
> 工程分层：[架构.md](./架构.md)  
> UI 调研：[UI调研.md](./UI调研.md)  
> **界面设计（讨论累计）**：[界面设计.md](./界面设计.md)

---

## 1. 产品定位

| 项 | 要求 |
|---|---|
| 产品名 | **安崽ETF** |
| 本质 | 个人股票 / ETF **分析助手**（不是交易下单工具） |
| 服务对象 | 本人个人使用；单人本地 / 私有部署 |
| 不做 | 多租户、社交、自动下单、公开展示型产品 |

**核心价值（原文意图）**

1. 为我个人股票提供 **实时分析** 与 **规划**
2. 一眼看清持仓与组合状态
3. 有 **Agent 智能分析助手**，结合持仓与行情做解释与建议

---

## 2. 市场与数据范围

| 项 | 已确认 |
|---|---|
| 市场 | **A 股** |
| 品种重心 | 场内 **ETF** 为主 |
| 持仓扩展 | 可含 A 股个股（录入与展示） |
| 不做（当前） | 美股、港股、跨境组合（除非日后追加） |

**行情相关要求**

- 准实时 / 盘中可刷新（打开 App 时轮询即可；iOS PWA 后台能力有限）
- 行情经后端代理，前端不直连第三方
- 数据源可切换（适配层）；失败可降级，不可让 App 崩溃
- 界面需标明：行情可能延迟；**非投资建议**

---

## 3. 体验与交互（PWA / iOS）

用户原话要点：**原生 PWA、iOS 全屏操作模式、流畅、iOS 交互模式、好看美化**

### 3.1 必须达到

- 可「添加到主屏幕」，以 **standalone / 全屏** 接近原生 App
- iOS 安全区正确（刘海、Home Indicator）
- 底部 Tab 导航，手势与滚动流畅
- 视觉要好看、有金融产品质感，不是通用模板脸

### 3.2 信息架构（五 Tab）

1. **仓库** — 个人持仓录入与市值盈亏  
2. **市场** — 自选 / 最新行情数据  
3. **新闻** — 市场要闻 + 持仓相关 + **兴趣**（自定关键词资讯）  
4. **分析** — 日更报告、结构与再平衡建议（原看盘异动延期并入）  
5. **安崽** — Agent 智能对话  

系统参数配置走 API `/admin`，不进 PWA 底栏。详见 [界面设计.md](./界面设计.md) §3。

### 3.3 体验细则

- 布局使用 `100dvh`、安全区 `env(safe-area-inset-*)`
- 盘中刷新间隔可调（如 15–30 秒）；切换 Tab 不丢状态
- 列表与图表要跟得上流畅度（虚拟化 / 按需加载）
- A 股涨跌色：**涨红跌绿**
- 视觉方向：炭黑 / 深墨绿底 + 清晰数字排版；避免紫渐变等 AI 通用风

---

## 4. 功能需求

### 4.1 持仓与交易记录

- 手动录入：代码、份额、成本
- 支持导入（CSV / 粘贴）与导出备份（后续）
- 计算：市值、盈亏、仓位占比、行业/宽基暴露（分析阶段加强）
- 本地持久化（SQLite）

### 4.2 实时分析与规划

用户原话：**为我个人股票提供实时分析，规划**

- **盘中**：涨跌、偏离成本、组合波动摘要
- **收盘 / 日报告**：收益归因、波动、相关性粗估、集中度
- **规划**：目标仓位模板 vs 当前仓位，给出 **调仓建议**（只建议，不下单）

### 4.3 Agent 智能助手（安崽）

用户原话：**有 agent 智能分析助手**

- 流式对话（SSE）；**多会话**：对话记录列表、关闭对话、新开对话；消息按 `conversation_id` 落库；设置里可清空全部
- **口吻**：心里专业、嘴上白话（少术语）；像微信，不播报、不推销；数字禁止 `**加粗**`，嵌进口语句子
- **硬约束**：数字只来自 Tool / 上下文，禁止瞎编；**可给倾向性买卖/调仓建议**（观望、轻仓、宜减不宜加等），禁止硬指令（立刻全仓/必须卖掉）、禁止承诺收益、不假装代下单

**已落地 Tools**：`get_indices` / `get_quote` / `get_intraday` / `get_kline` / `get_sector` / `get_macro` / `get_portfolio` / `get_news` / `search_symbol` / `get_analysis_snapshot` / `get_leaders`；问代码或名称时自动深取；榜单/报告按需预取。

**场景包（`agent_scene`）**：按本轮意图投喂，不每轮固定长模板。
- 场景：`chat` / `macro` / `market` / `stock` / `portfolio` / `leaders` / `analysis` / `news`
- 人设薄、固定；仓库 / 分析师 Skill / 场景说明按场景开关
- 例：只问黄金 → 只带宏观+日历，不带仓库；问仓位 → 才带仓库摘要
- **动态篇幅**：`max_tokens` + 篇幅提示按场景/问句长短调整（闲聊短、个股深挖较长），不每轮锁死同一字数

**待加（按价值排序，触发再做）**

| 能力 | 说明 |
|---|---|
| `draft_rebalance_plan` | 调仓草案（结构化，仍禁止下单话术） |
| 财务摘要 Tool | 营收/净利/估值关键项（有可靠源再接） |
| 资金/龙虎榜（轻） | 仅摘要，盘后可用 |
| 自选对比 | 持仓 vs 自选，白话对比 |
| 风险偏好设定 | 设置页写入，注入提示词 |

**人性化（冻结）**：称呼稀薄；禁四段套路；术语译成人话；工具返回文案本身偏口语。

### 4.4 自选与提醒（二期）

- 自选列表（一期已有雏形）
- 价格 / 涨跌阈值提醒；iOS PWA Push 弱，可用应用内 + Bark / 邮件

---

## 5. 技术选型（已确认）

| 层 | 选型 | 备注 |
|---|---|---|
| 前端 | Next.js（App Router）+ TypeScript + Tailwind | 纯 PWA 壳与 UI |
| 后端 | **FastAPI**（用户确认合适） | 持仓 / 行情 / 分析 / Agent 全走后端 |
| LLM | OpenAI **兼容** API | OpenAI / DeepSeek / 通义等，env 配置 |
| 数据库 | SQLite + SQLAlchemy | 个人零运维 |
| 行情 | 适配器（现用新浪等公开源，可换 akshare 等） | 缓存 + 限流意识 |
| 编排 | 本地双进程；可选 docker-compose | |

**分工铁律**

- Next.js **不做**业务 API
- 密钥（LLM、口令等）只放后端 / env，不进前端明文仓库习惯用法

**鉴权**

- 本机 / 私有访问口令（`APP_PASSWORD` / `X-App-Password`），非完整账号体系

---

## 6. 合规与边界

- 显著位置标注：**分析与建议不构成投资建议**
- 不提供自动交易、不承诺收益
- 公开行情源可能限流或不稳定 → 必须可降级、可换源
- iOS Safari PWA：后台刷新与 Push 受限 → 「实时」定义为打开 App 时的准实时

---

## 7. 成功标准（MVP）

1. iPhone 加到主屏幕后全屏可用，安全区与底部导航正确  
2. 可录入持仓并看到当日盈亏与仓位结构（数据来自 FastAPI）  
3. 能生成一页可读的组合分析 / 调仓建议  
4. Agent 能结合真实持仓回答，并给出可核对的数据引用  

---

## 8. 分阶段落地（对照备忘）

| 阶段 | 内容 | 状态（备忘） |
|---|---|---|
| Phase 0 | 骨架：PWA + FastAPI + 持仓 CRUD + Tab | 已启动 / 已搭 |
| Phase 1 | 行情与持仓看板打磨、自选 | 待做 |
| Phase 2 | 分析报告与规划（再平衡） | 待做 |
| Phase 3 | Agent「安崽」SSE + Tools | 进行中：深取 Tools + 白话 Skill；缺口见 §4.3 待加表 |
| Phase 4 | iOS 手势动画、离线壳、导入导出、提示词迭代 | 持续 |

---

## 9. 用户原话索引（便于回溯）

1. 原生 PWA，iOS 全屏操作模式，流畅，iOS 交互模式，好看美化  
2. 本项目为「安崽ETF」，个人股票分析助手  
3. 为我个人股票提供实时分析、规划  
4. 有 Agent 智能分析助手  
5. 市场：A 股  
6. 技术：Next.js PWA + 自建后端 + OpenAI/兼容 API；后端再确认为 **FastAPI**  

---

## 10. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-05 | 初版：汇总启动需求与已确认选型 |
| 2026-08-05 | 按 iOS 全屏分析完成壳层/弹窗/Tab 保活优化 |
| 2026-08-05 | iOS 设计文档合并为《iOS全屏设计.md》 |
| 2026-08-05 | 七方面最佳实践检索并导入（启动图/视口/转场等） |
| 2026-08-05 | 交互小细节盘点并打底《交互基础规范》+ 令牌/组件 |
| 2026-08-05 | 架构约定固化：权威路径 + React Query/测试触发条件（见《架构.md》） |
| 2026-08-05 | FastAPI `/admin` 管理后台（配置不进 PWA） |
| 2026-08-05 | 底栏改为五 Tab：仓库/市场/看盘/分析/安崽 |
| 2026-08-05 | 建立《界面设计.md》：图标来源（Lucide）+ 页面架构参考方法与四 Tab 骨架 |
| 2026-08-05 | 看盘改为新闻：市场要闻+持仓相关；看盘异动延期并入分析 |
