DATABASE_URL=sqlite:///./anzai.db
# Knowledge base only (Postgres + pgvector). Business data stays on DATABASE_URL.
KNOWLEDGE_DATABASE_URL=postgresql://postgres:postgres@192.168.2.38:5437/anzai_knowledge
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
# Analysis committee (separate from 安崽 chat). Prefer /admin/analysis-llm; or set here.
LLM_ANALYSIS_BASE_URL=
LLM_ANALYSIS_API_KEY=
LLM_ANALYSIS_MODEL=
# Embedding connection (separate from chat). Prefer /admin/embedding UI; or set here.
LLM_EMBEDDING_BASE_URL=
LLM_EMBEDDING_API_KEY=
LLM_EMBEDDING_MODEL=text-embedding-v4
APP_PASSWORD=
# Local account reminder (hashed password lives in DB; sync via /admin or script)
DEMO_USERNAME=poilly
DEMO_PASSWORD=
QUOTE_PROVIDER=sina
CORS_ORIGINS=http://localhost:3515,http://127.0.0.1:3515,https://anzai.605081.xyz:16666
# Optional: stable cookie secret for /admin (else derived from APP_PASSWORD)
ADMIN_SESSION_SECRET=
